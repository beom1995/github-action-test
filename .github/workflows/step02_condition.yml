name: step02_condition
on: workflow_dispatch
jobs:
  build:
    if: github.repository == 'beom1995/github-action-test'
    runs-on: ubuntu-latest
    steps:
    - name: Print Result
      run: |
        echo "Run Build on beom1995/github-action-test"
